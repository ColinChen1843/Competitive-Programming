#include <bits/stdc++.h>

using namespace std;

int main() {
  string s;
  cin>>s;
  
  if(s.find("CCC") < s.size()){
    cout<<"NO"<<endl;
  }

  else{
    cout<<"YES"<<endl;
  }

  return 0;
}